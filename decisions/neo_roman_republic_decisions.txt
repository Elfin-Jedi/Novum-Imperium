decisions = {
	restore_roman_republic = {
		only_independent = yes
		
		is_high_prio = yes
		ai_check_interval = 120
		
		potential = {
			government = merchant_republic_government
			culture = roman
			capital_scope = { region = world_europe_south_italy }
			has_global_flag = flag_started_roman_renaissance
			NOR = {
				has_global_flag = rome_restored
				has_global_flag = roman_republic_restored
			}
		}
		
		allow = {
			has_landed_title = e_italy
			prestige = 5000
			war = no
			ruled_years = 10
			OR = {
				religion_group = christian
				religion_openly_hellenic_or_reformed_trigger = yes
			}
			is_heretic = no
			culture = roman
			capital_scope = { province = 333 }
		}
		
		effect = {
			e_italy = { #Entirely cosmetic change - you're ultimately still just Italia. No access to Reconquest CB, Byzantines can still go for it, etc etc
				set_name = "Roman Republic"
				adjective = "Roman"
				set_coa = e_roman_republic
				set_color = { 255 0 0 } #This is just an experimental colour to see if this works
			}
			
			set_global_flag = roman_republic_restored #Doesn't count as a 'legitimate' restoration of the Empire. Since you're not. Thus, no Roman Restoration flag set.
			narrative_event = { id = NIR.08 } #Notification event
			any_playable_ruler = { limit = { ai = no } narrative_event = { id = NIR.08 } } #Make sure to include unique responses for the Romes
		}
		
		revoke_allowed = { always = no }
		
	}
	
	promote_roman_culture_republic = {
		only_independent = yes
		
		is_high_prio = yes
		ai_check_interval = 120
		
		potential = {
			government = merchant_republic_government
			capital_scope = { region = world_europe_south_italy }
			is_adult = yes
			is_incapable = no
			OR = {
				religion_openly_hellenic_or_reformed_trigger = yes
				religion_group = christian
			}
			NOT = { has_global_flag = flag_started_roman_renaissance }
		}
		allow = {
			has_landed_title = c_roma #Has to personally hold Rome. Capital req. meant only E_italy or D_latium primaries could do it - not the intended progression.
			culture_group = latin
			learning = 15
			completely_controls = d_latium
			war = no
			wealth = 2000 #This can act as a gateway to the player needing to either expand or play REALLY tall, both work
			OR = {
				religion_group = christian
				religion_openly_hellenic_or_reformed_trigger = yes
			}
		}
		effect = {
			custom_tooltip = {
				text = tooltip_roman_culture_spreads
				roman_culture_convert_effect = yes
			}
			set_global_flag = flag_started_roman_renaissance
			wealth = -2000
		
			narrative_event = { id = NIR.13 }
			any_playable_ruler = { limit = { ai = no } narrative_event = { id = NIR.13 } }
		}
		
		revoke_allowed = { always = no }
		
		ai_will_do = {
			factor = 1
			modifier = {
				factor = 3
				religion = hellenic_pagan_reformed
			}
		}
	}
	
	adopt_roman_culture_republic = {
		only_playable = yes
		ai_check_interval = 120
		
		potential = {
			has_global_flag = flag_started_roman_renaissance
			top_liege = {
				AND = {
					has_landed_title = e_italy
					culture = roman
				}
			}
			NOT = { culture = roman }
		}
		allow = {
			show_only_failed_conditions = yes
			OR = { 
				culture_group = latin 
				culture_group = byzantine
			}
		}
		
		effect = {
			custom_tooltip = {
				text = tooltip_roman_culture_spreads
				roman_culture_convert_effect = yes
			}
		}
		
		revoke_allowed = { always = no }
		
		ai_will_do = {
			factor = 1
			modifier = {
				factor = 3
				religion = hellenic_pagan_reformed
			}
		}
	}
}